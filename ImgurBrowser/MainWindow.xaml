<Window x:Class="Miq.ImgurBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:data="clr-namespace:Miq.ImgurBrowser.Data"
        Title="Imgur Browser"
        Background="Black">

    <Window.Resources>
        <Style x:Key="ImagesStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <WrapPanel IsItemsHost="True"
                                   Orientation="Horizontal"
                                   VerticalAlignment="Top"
                                   HorizontalAlignment="Stretch"
                                   ItemWidth="90"
                                   ItemHeight="90" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate DataType="{x:Type data:Image}">
            <Image Source="{Binding Bitmap}" />
        </DataTemplate>
    </Window.Resources>

    <ListBox Style="{StaticResource ImagesStyle}"
             DataContext="{Binding Source={StaticResource Images}}"
             ItemsSource="{Binding}">
    </ListBox>
</Window>
